part of '../home_view.dart';

final class _ErrorTextWidget extends StatelessWidget {
  const _ErrorTextWidget();

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text(AppText.error));
  }
}
